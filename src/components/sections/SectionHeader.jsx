import SectionTitle from '../ui/SectionTitle';

export default function SectionHeader({ eyebrow, title, description }) {
  return <SectionTitle eyebrow={eyebrow} title={title} description={description} />;
}
